// Re-exported from allergen-catalog — single source of truth per ADR-0017.
// DEFAULT_TESTED_ALLERGENS is a protocol policy constant; it lives in $lib/domain/policy.
export { CATEGORIES } from '$lib/data/allergen-catalog/index';
