/**
 * Alias resolution tests covered the old free-text custom food input removed in issue #244.
 * The feature (alias → canonical food mapping) is deferred to a sibling slice.
 * Tests removed to avoid false failures; re-add when the custom food input is reintroduced.
 */
